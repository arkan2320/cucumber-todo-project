# ده "اسم الفرح" اللي هيظهرلك في قائمة الـ Actions
name: run cucumber tests

# دي "ساعة الصفر": امتى الروبوت ده يصحى ويشتغل؟
on:
  push:
    branches: ["master"] # لما حد يعمل (Push) ويرفع كود جديد على فرع "master"

jobs:
  test:
    # دي "أرض المعركة": بنقول لـ GitHub أجرلنا جهاز مواصفاته إيه؟
    # هنا طلبنا جهاز "ويندوز" أحدث إصدار (زي جهازك بالظبط عشان متتعبش)
    runs-on: windows-latest

    steps:
      # الخطوة 1: "هات العدة"
      # الروبوت بيروح المخزن (Repository) ويسحب نسخة من الكود بتاعك يحطها على الجهاز المؤجر
      - uses: actions/checkout@v3

      # الخطوة 2: "جهز العدة"
      # بنقوله سطب Java 21 عشان الكود بتاعنا مكتوب بيها
      - uses: actions/setup-java@v3
        with:
          java-version: '21'
          cache : maven        # احتفظ بالمكتبات عشان منحملهاش كل مرة (توفير وقت)
          distribution: 'temurin' # ده نوع مشهور ومحترم من الجافا

      # الخطوة 3: "جهز العربية"
      # بنقوله سطب متصفح Google Chrome عشان السيلينيوم يعرف يفتحه
      - uses: browser-actions/setup-chrome@latest

      # الخطوة 4: "دوس بنزين!" 🚗💨
      # دي أهم خطوة، بنرن أمر الميفن عشان يبدأ التست الفعلي
      - run: mvn clean test -Dbrowser="chrome"